############################## CONFIG FILE ##################################

# Author(s) : Kenzo Hillion
# Contact : kenzo.hillion@curie.fr
# Comment(s) : CONFIG file for mapping of reads with different methods :
#	 : Mapping to reference genome
#	 : Mapping to parental genomes (both chromosomes separatly)
#	 : Mapping to diploid genome (both parental chromosomes in the same time)
#	 : Mapping to reference N-masked genome

# ------------ Tool paths ------------

# Required installed tools
PYTHON=/bioinfo/local/build/python/python-2.7.9/bin/python2.7
SAMTOOLS_DIR=/bioinfo/local/build/samtools/samtools-1.3/bin
#CHECK_VARIANTS=/bioinfo/users/khillion/bin/clinTools/check_variants.py
SNPSPLIT_DIR=/data/annotations/Mouse/personalized_genomes/SNPsplit

# Local scripts
#	Paths to the directory with the different scripts
PIPELINE_PATH=/bioinfo/users/khillion/GIT_dev/as-mapping/mapping_pipeline

#   Update paths and local scripts
source ${PIPELINE_PATH}/ALL_PATHS
 
# ------------ Input(s) ------------

# Files or Directories
#   Path of the reads to map (either .fq or .fq.gz)
#   Only fill up FQ_READS_F for single-end
FQ_READS_F=
FQ_READS_R=
#   full_vcf : latest VCF file downloaded from Sanger
FULL_VCF=/data/annotations/Mouse/variant_informations/mgpV5_mm10/mgp.v5.merged.snps_all.dbSNP142.vcf

#   Full reference genome (fasta format)
REF_GENO=/data/annotations/Mouse/mm10/complete/mm10.fa
B2_INDEX_REF=
#   Directory containing every chromosomes of the reference genome at the fasta format
#   (Ensembl annotation)
REF_DIR=/data/annotations/Mouse/mm10/chromosomes_Ensembl/

# 	Strain specific informations
# First genotype
ID_GENO1=CAST_EiJ
FASTA_GENO1=
B2_INDEX_GENO1=
# Second genotype
ID_GENO2=129S1_SvImJ
FASTA_GENO2=
B2_INDEX_GENO2=
# Diploid
FASTA_DIP=
B2_INDEX_DIP=
# N-masked
FASTA_NMASK=
B2_INDEX_NMASK=
# SNP file
SNP_FILE=

#   Selected strategies
#   [0] to skip the strategy
#   [1] to select the strategy
MAP_REF=0
MAP_N=0
MAP_PAR=0
MAP_DIP=0

#   Selected mapper
#    [1] BOWTIE2
#    [2] TOPHAT (with Bowtie2)
MAPPER=BOWTIE2

# ------------ Output(s) ------------

# Output directories for generated files to be reused (N masked genome, indexes ...)
GEN_OUT=GENERATION_DIR
#	Folders for different file type
FASTA_OUT=${GEN_OUT}/fastas
INDEXES=${FASTA_OUT}/indexes
VCF_OUT=${GEN_OUT}/vcfs

OUT_DIR=MAPPED_OUTDIR

#   Name for the output bam
OUT_NAME=MY_ANALYSIS

# ------------ Bowtie2 Parameters ------------
BOWTIE2_DIR=/bioinfo/local/build/bowtie2/bowtie2-2.2.5
# -- Options
B2_OPT="--reorder -p 8 --very-sensitive"


# ------------ Tophat Parameters ------------
TOPHAT_DIR=/bioinfo/local/build/tophat/tophat-2.1.0
# -- Annotation file
TOPHAT_GTF=
TOPHAT_TRANSC_INDEX=
# -- Options
TOPHAT_OPT="-p 8 -g 1"
# -- Bowtie2 specific options
TOPHAT_B2_OPT="--b2-very-sensitive"
